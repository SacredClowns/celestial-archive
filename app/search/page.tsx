export const metadata = {
  title: "Search · Celestial Archive"
};

export default function SearchPage() {
  return (
    <section className="mx-auto max-w-[720px] py-16 text-center text-gold-dim">
      <p>Use ⌘K or Ctrl+K anywhere in the Archive to search.</p>
    </section>
  );
}
